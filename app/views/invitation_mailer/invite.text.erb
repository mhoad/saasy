You have been invited to join the <%= @invitation.account_name %> account
on <%= Rails.configuration.application_settings['application_name'] %>
<% accept_url = accept_invitation_url(
  @invitation, subdomain: @invitation.account_subdomain) %>
<%= accept_url %>